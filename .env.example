# ==============================================================================
# GLINAX CHATBOT - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to .env and fill in the values for your environment
#
# IMPORTANT:
# - Never commit the actual .env file to version control
# - Use strong, unique values for secrets
# - Different environments (dev, staging, prod) should have different secrets
#
# ==============================================================================

# ============== FRONTEND CONFIGURATION (glinax-chatbot/) ==============

# API Base URL for frontend to communicate with backend
# Development: http://localhost:5000/api
# Production: https://api.yourdomain.com/api
VITE_API_BASE_URL=https://glinax-backend.onrender.com/api

# Enable debug mode in development (shows verbose logs)
VITE_DEBUG_MODE=true

# ============== BACKEND CONFIGURATION (ai-chatbot-backend/) ==============

# Server Port (default: 5000)
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=glinax_chatbot

# JWT Secret for token generation and verification
# Use a strong, random string (min 32 characters recommended)
JWT_SECRET=your_very_secure_jwt_secret_key_min_32_chars_long

# Email Configuration (for verification emails)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Paystack Payment Gateway Configuration
PAYSTACK_PUBLIC_KEY=your_paystack_public_key
PAYSTACK_SECRET_KEY=your_paystack_secret_key
PAYSTACK_WEBHOOK_URL=https://yourdomain.com/api/payments/webhook

# Frontend URL (for CORS configuration)
FRONTEND_URL=http://localhost:3000

# ============== AI/RAG SERVICE CONFIGURATION (ai-rag-service/) ==============

# Python AI Service URL
# Development: http://127.0.0.1:8000
# Production: https://ai-service.yourdomain.com
AI_SERVICE_URL=http://127.0.0.1:8000/respond

# LLM Provider Configuration
# Supported: groq, openai
LLM_PROVIDER=groq

# Groq API Key (for RAG/LLM responses)
GROQ_API_KEY=your_groq_api_key

# OpenAI API Key (alternative LLM provider)
OPENAI_API_KEY=your_openai_api_key

# Search Service Configuration
SERPAPI_API_KEY=your_serpapi_key  # For web search in RAG service

# MongoDB for AI Service
# MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/glinax_chatbot
# JWT_SECRET_AI=your_jwt_secret_for_ai_service

# ============== OPTIONAL FEATURES ==============

# Enable/Disable specific features
ENABLE_PAYMENTS=true
ENABLE_FILE_UPLOADS=true
ENABLE_REAL_TIME_NOTIFICATIONS=true
ENABLE_EMAIL_VERIFICATION=true
ENABLE_RATE_LIMITING=true

# Log Level (debug, info, warn, error)
LOG_LEVEL=info

# ============== NOTES ==============
# 
# MongoDB Atlas Setup:
#   1. Create account at https://www.mongodb.com/cloud/atlas
#   2. Create a cluster
#   3. Get connection string and replace MONGODB_URI
#
# Paystack Setup:
#   1. Create account at https://paystack.com
#   2. Get API keys from dashboard
#   3. Configure webhook in Paystack dashboard pointing to /api/payments/webhook
#
# Groq API Setup:
#   1. Sign up at https://console.groq.com
#   2. Get API key from console
#
# Email Setup (Gmail):
#   1. Enable 2-factor authentication on your Google account
#   2. Generate app password at https://myaccount.google.com/apppasswords
#   3. Use app password in EMAIL_PASSWORD (not your actual password)
#
# ==============================================================================
